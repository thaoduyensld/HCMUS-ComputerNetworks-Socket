# Runtime data

Thư mục này mô tả dữ liệu sinh khi chạy ứng dụng:

- `server_storage/`: file hoàn chỉnh của Server.
- `client_downloads/`: file Client tải xuống.
- `logs/`: log runtime.

Nội dung runtime, file `.part`, log và file test lớn không được commit vào Git.
